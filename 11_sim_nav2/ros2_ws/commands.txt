# Frequently used commands inside the container's browser desktop terminal(s)
# (http://localhost:6080). Not for your host machine.
# Pasted in order for what you should need


# --- Build the package after editing files ---
# Note: you only need to do this once; built files are stored on your computer outside container
cd ~/ros2_ws
colcon build --symlink-install

# --- Launch Gazebo ---
cd ~/ros2_ws
source install/setup.bash
ros2 launch goosebot_0 spawn_robot.launch.py world:=$(ros2 pkg prefix goosebot_0)/share/goosebot_0/worlds/maze.world


# --- Run the navigation script (second terminal) ---
cd ~/ros2_ws
source install/setup.bash
ros2 launch goosebot_0 nav2_bringup.launch.py map:=/home/ubuntu/ros2_ws/src/goosebot_0/maps/maze.yaml

# --- Open RViz for visualization of the navigation algorithm ---
ros2 run rviz2 rviz2 -d ~/ros2_ws/default_config.rviz


# --- Send a target for the robot to navigate to (fourth terminal) ---
ros2 action send_goal /navigate_to_pose nav2_msgs/action/NavigateToPose "{pose: {header: {frame_id: 'map'}, pose: {position: {x: 2.0, y: -4.5, z: 0.0}}}}" --feedback





# OTHER COMMANDS YOU MAY NEED:


# --- Build only a specific package (ex: goosebot_0) ---
colcon build --packages-select goosebot_0

# --- Launch Gazebo with your custom robot (default world) ---
ros2 launch goosebot_0 spawn_robot.launch.py

# --- Drive the robot with the keyboard (second terminal) ---
ros2 run teleop_twist_keyboard teleop_twist_keyboard


cd ~/ros2_ws
source install/setup.bash
ros2 launch goosebot_0 nav2_bringup.launch.py 2>&1 | tee ~/nav2_launch.log
